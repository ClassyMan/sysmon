[package]
name = "cpu"
version = "0.1.0"
edition = "2024"
description = "CPU monitor with per-core utilization charts"
license = "MIT"

[dependencies]
sysmon-shared = { path = "../shared" }
ratatui = "0.30"
crossterm = "0.29"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
